# Data-Structures-Labs
An implementation of two data structures: a singly linked list (stack style) and a doubly linked list (queue style). 
Both implementations contain an interactive interface containing basic functions for working with the list.