# Singly Linked List in C++
A simple implementation of a singly linked list (stack style) with the following features:
* Push/Pop: Standard LIFO operations.
* Delete Max: Custom function to find and remove the largest elements.
* Interactive Menu: User-friendly console interface.